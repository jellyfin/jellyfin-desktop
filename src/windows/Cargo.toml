[package]
name = "jfn-windows"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "jfn_windows"
crate-type = ["rlib"]

[target.'cfg(target_os = "windows")'.dependencies]
jfn-platform-abi = { path = "../platform_abi" }
