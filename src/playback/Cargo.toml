[package]
name = "jfn-playback"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "jfn_playback"
crate-type = ["rlib"]

[dependencies]
jfn-wake-event = { path = "../wake_event" }
libc = "0.2"
