[package]
name = "jfn-signal-guard"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "jfn_signal_guard"
crate-type = ["rlib"]

[target.'cfg(unix)'.dependencies]
libc.workspace = true
