[package]
name = "jfn-signal-guard"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "jfn_signal_guard"
crate-type = ["staticlib"]

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
panic = "abort"
strip = true
